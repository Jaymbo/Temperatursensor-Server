server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing (API + WebSocket gehen direkt an :8000)
    location / {
        try_files $uri $uri/ /index.html;
    }
}